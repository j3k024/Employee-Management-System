# Employee-Management-System
The Employee Management System is a Python CLI tool for HR data. It enables adding records with unique ID checks, updating specific details via submenus, and deleting entries. All data is stored in a list of dictionaries and displayed in a tabular format. The program runs in a continuous loop until the user selects Exit.
